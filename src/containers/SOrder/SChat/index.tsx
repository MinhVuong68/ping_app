import React from 'react';
import { Text, View } from 'react-native';

import { Layout } from '../../../theme';
import { Header } from '../../../components';

const SChat = () => {
  return (
    <View style={Layout.full}>
      <Header title="Nhắn tin với nhân viên giao hàng" />
        
    </View>
  );
};

export default SChat;
